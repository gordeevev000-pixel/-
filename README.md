# Движки SPRING и COMPASS для TradingView

Два скоринговых движка на Pine Script v6. Оба раскладывают решение на взвешенные компоненты,
показывают план сделки на графике и считают таблицу калибровки — что каждый диапазон скора
реально дал на вашем инструменте.

| движок | таймфрейм | отвечает на вопрос |
|---|---|---|
| [**SPRING**](#spring--движок-отскоков) | 5 мин | «здесь отскок?» — ловит возврат от растянутого экстремума |
| [**COMPASS**](#compass--лонг-или-шорт-1-мин) | 1 мин | «куда сейчас смотреть — лонг, шорт или никуда?» |

---

# SPRING · движок отскоков

Порт движка отскоков **SPRING** на Pine Script v6: скор, состояния, готовый план сделки
и таблица калибровки прямо на графике.

```
Score = 100 · gain · G · ( 0.20·S + 0.22·P + 0.28·R + 0.13·I + 0.17·N )
```

| | компонент | что меряет |
|---|---|---|
| **S** | растяжение | насколько цена ушла от средней — одновременно в σ и в ATR |
| **P** | истощение | климакс объёма, затухание импульса, хвост свечи |
| **R** | возврат | свип уровня и реклейм — **триггер**, только текущий бар |
| **I** | дивергенция | RSI + дельта объёма (CVD) + необязательный коррелят |
| **N** | уровни | конфлюэнс EMA / VWAP / хай-лоу дня / круглого числа / фибо в допуске 0.28 ATR |
| **G** | режим | множитель 0…1: душит сетапы против сильного тренда и в разгоне волатильности |

Контекст копится на экстремуме, а триггер приходит через несколько баров. Поэтому
**S/P/I/N берутся как максимум за окно контекста, а R считается строго по текущему бару.**
Если требовать совпадения всего на одном баре, сигналов не будет вообще.

## Файлы

| файл | что делает |
|---|---|
| `pine/SPRING.pine` | индикатор: панель скора, состояния FLAT → WATCH → ARMED → TRIGGER, план сделки на графике, таблица компонентов, таблица калибровки, алерты |
| `pine/SPRING_strategy.pine` | стратегия: те же сигналы, но исполняет их движок TradingView — нативный отчёт с эквити, просадкой и списком сделок |
| `pine/COMPASS.pine` | индикатор направления для минутки: вердикт ЛОНГ / ШОРТ / ЖДАТЬ, разбор по компонентам, план сделки, фильтры-вето, калибровка |

Ядро (компоненты, скор, план) в обоих файлах одинаковое — второй файл собран из первого,
так что правки в логике надо повторять в обоих.

## Установка

1. TradingView → **Pine Editor** (внизу экрана) → **Открыть → Новый индикатор**.
2. Удалить заготовку, вставить содержимое `pine/SPRING.pine`, **Сохранить**, **Добавить на график**.
3. Индикатор рисуется отдельной панелью снизу (скор), а план сделки и стрелки — поверх свечей.
   Панель можно растянуть за нижнюю границу, чтобы влезли таблицы.
4. Стратегию добавлять отдельным скриптом — она сама рисуется поверх графика,
   отчёт открывается вкладкой **Тестер стратегии**.

Рабочий таймфрейм — тот же, на котором считалась калибровка: 5 минут на фьючерсах и индексах.
На других инструментах и таймфреймах параметры надо пересчитывать (см. ниже).

## Как читать

**Панель.** Заливка — скор, сплошная линия — он же в цвете состояния, тонкая линия сверху —
*потенциал*: какой скор получился бы при идеальном возврате прямо сейчас. Пунктир — порог входа,
точечная линия — порог наблюдения.

**Состояния.**
* `FLAT` — торговать нечего.
* `WATCH` — контекст набирается.
* `ARMED` — контекст готов: идеальный возврат на этом баре дал бы вход. Здесь ставят лимитку.
* `TRIGGER` — скор перешёл порог, сигнал.

**План сделки** рисуется на графике при сигнале и держится, пока сделка открыта:
вход, стоп за свингом с запасом 0.5 ATR, T1 на средней, T2 = 2.6R, лимитка на ретесте
(стоп ∓ 0.3 ATR), размер позиции = скор / риск в ATR.

**Таблица компонентов** справа вверху раскладывает скор обратно на слагаемые: значение × вес = вклад.
Если сделка не нравится — видно, какая именно строка её вытянула.

## Калибровка — главное

Абсолютное значение скора само по себе не значит ничего. Значение имеет таблица справа внизу:
что каждый диапазон скора дал **на вашем инструменте**. Индикатор прогоняет симулятор по всей
видимой истории, берёт сделки от «нижнего порога для калибровки» (по умолчанию 50, а не 70 —
чтобы было видно, с какого диапазона начинается эдж) и считает по каждому диапазону сделки,
винрейт, профит-фактор, средний R и среднее число баров в сделке.

Порог входа берут не из головы, а отсюда: он ставится там, где кончается рост среднего R.
**Если винрейт и средний R не растут вместе со скором — на этом инструменте модель не работает,
какой бы стройной она ни была.** Комиссия и проскальзывание задаются полем
«Комиссия и проскальзывание, тиков на сделку» и вычитаются из каждой сделки.

Симулятор — не бэктестер: он считает по барам, при касании стопа и цели на одном баре
засчитывает стоп, и не знает про частичные исполнения. Для честного отчёта используйте
`SPRING_strategy.pine` и вкладку «Свойства» с реальной комиссией и проскальзыванием.

## Параметры, которые действительно стоит крутить

| параметр | зачем |
|---|---|
| **Средняя** (20 EMA) | цель T1 и точка отсчёта растяжения. Длиннее — реже и крупнее сетапы |
| **Окно контекста** (12) | сколько баров живёт контекст до триггера |
| **Порог входа** (70) | ставится по таблице калибровки, а не по вкусу |
| **Полное растяжение** σ / ATR (2.5 / 2.5) | что считать «далеко от средней» на вашем инструменте |
| **Климакс объёма** (2.5×) | для индексов без реального объёма — снизить или не полагаться на P |
| **Допуск конфлюэнса** (0.28 ATR) | шире — N срабатывает чаще и хуже различает |
| **Тайм-стоп** (12 баров) | средняя сделка живёт ~3 бара; тайм-стоп режет зависшие |
| **G: ER тренда** (0.20 / 0.60) | насколько сильно душить вход против тренда |

Веса компонентов вынесены в отдельную группу — их сумма должна оставаться равной 1,
иначе поедет шкала скора и порог придётся калибровать заново.

## Алерты

Три готовых условия: `SPRING · LONG`, `SPRING · SHORT`, `SPRING · любой сигнал`.
Плюс вызов `alert()` с полным планом в тексте — выбрать тип алерта
**«Любой вызов функции alert()»**, и в сообщение придёт сторона, скор, вход, стоп, T1, T2,
R:R, размер и цена лимитки на ретесте.

Ставьте алерты на **«Раз за бар на закрытии»**. При выключенной галке «Сигналы только по закрытым барам»
сигнал появляется внутри формирующегося бара и может исчезнуть — это нормальное поведение
любого триггерного скрипта, но на живых деньгах так торгуют осознанно.

## Про заглядывание в будущее

Все компоненты считаются по закрытым данным текущего бара. Хай/лоу предыдущего дня берутся
через `request.security(..., lookahead_on)` с явным сдвигом `[1]` — это штатный способ получить
вчерашние уровни без подглядывания. VWAP сессии считается вручную, а не через `ta.vwap`,
чтобы скрипт не падал на инструментах без объёма; там же, где объёма нет, компонент P
переходит на нейтральное значение, а CVD-часть дивергенции отключается.

## Оговорка

Числа из исходной презентации SPRING (3 877 сделок, профит-фактор 1.16) получены на
**синтетических** данных: генератор с переключением режимов и встроенным возвратом к средней.
Это доказывает, что механика верна и нет заглядывания в будущее, но **это не доказательство
эджа на реальном рынке** — генератор сам благосклонен к отскоковой логике.

Прежде чем ставить деньги, прогоните таблицу калибровки на своей истории и посмотрите на свою
таблицу. Компоненты здесь — добросовестная реконструкция по опубликованным определениям
(«растяжение в σ и ATR», «климакс объёма и затухание», «свип и реклейм», «конфлюэнс 0.28 ATR»),
а не побайтовый порт закрытого `assistant.py`: арифметика плана сходится с оригиналом точно,
формулы компонентов — по смыслу. Поэтому пороги и веса на вашем инструменте почти наверняка
придётся подобрать заново.

---

# COMPASS · лонг или шорт (1 мин)

Минутный советник направления. Не даёт сигналов на каждом баре — даёт **вердикт**:
ЛОНГ, ШОРТ или ЖДАТЬ, с разбором, откуда он взялся, и с планом сделки.

```
Trend = 100 · ( 0.24·H + 0.17·T + 0.19·M + 0.13·V + 0.15·I + 0.12·D )
Fade  = 100 · F
Score = τ · Trend + (1 − τ) · Fade
```

| | компонент | что меряет |
|---|---|---|
| **H** | старший контекст | 5m / 15m / 60m: стек EMA, положение цены, наклон — взвешенно |
| **T** | локальный тренд | стек EMA 9/21/50 и наклон средней, нормированный на ATR |
| **M** | структура | слом уровня (BOS) + качество свингов: HH/HL против LH/LL |
| **V** | VWAP | сторона, наклон, растяжение в σ — перелёт штрафуется |
| **I** | импульс | гистограмма MACD, её ускорение, RSI, ROC на ATR |
| **D** | давление | дельта объёма (CVD-прокси) + перевес растущих баров |
| **F** | фейд | растяжение от VWAP, RSI на краю, свип с реклеймом, дивергенция, истощение |
| **τ** | режим | сила тренда 0…1 из ADX, эффективности хода и Choppiness |

## Главная идея: две логики вместо одной

Обычный «индикатор направления» ломается там, где рынок меняет характер: трендовая логика
в диапазоне пилит, возвратная — попадает под тренд. COMPASS считает **обе** и смешивает их
по τ — измеренной силе тренда.

Побочный эффект важнее основного. На переходе (τ ≈ 0.5) тренд и фейд смотрят в разные стороны,
взвешенная сумма схлопывается к нулю, скор не добирает до порога — и движок честно говорит
**ЖДАТЬ**. Сигнал остаётся только там, где обе логики не спорят. Это не фильтр, приклеенный
сверху, а свойство самой формулы.

## Фильтры-вето

Скор отвечает на «куда», фильтры — на «стоит ли вообще». Они не двигают скор, они **запрещают
вердикт** и пишут в панель причину:

* **вола ниже нормы** — ATR по перцентилю своего окна ниже порога: на минутке спред и комиссия
  съедят такой ход;
* **пустой объём** — RVOL ниже порога;
* **пила** — Choppiness выше 61.8 (перебивается скором выше 55);
* **шпилька** — бар шире 3 ATR: новость или прокол, следующие 3 бара движок молчит;
* **вне сессии** — по желанию, свои торговые часы.

Смещение при этом никуда не девается: в панели видно «смещение есть, но вход запрещён»
и строку с причиной. Как только фильтр отпустит, вердикт появится сам.

Отдельно стоит **срез контртренда**: фейд против сильного старшего ТФ (|H| ≥ 0.55) режется
множителем 0.45. Ловить отскок против 15-минутного тренда — самая дорогая ошибка на минутке.

## Как читать панель

**Вердикт** — большая строка сверху, фон красится по уверенности. **Шкала** под ней —
скор от −100 (слева, шорт) до +100 (справа, лонг).

**Уверенность** считается из двух вещей: насколько велик скор (60%) и сколько компонентов
из шести согласны с его знаком (40%). Скор +45 при согласии 6/6 — это не то же самое,
что +45 при 3/6, хотя цифра одна.

**Что делать** — плейбук: `откат в тренде — зона входа здесь`, `продолжение после слома`,
`свип и реклейм`, `фейд от края диапазона`, `тренд есть, ждать отката`,
`переход режима — половина размера`. Зона отката рисуется на графике прямоугольником
между EMA 21 и VWAP.

**Компоненты** — значение, мини-шкала и вклад в трендовый скор. Сразу видно, какая строка
тянет вердикт и какая против.

**Σ тренд / Σ фейд** — два скора до смешивания и их множители (×τ и ×(1−τ)). Если оба
слагаемых большие, но разного знака — вот он, переход.

**План** — вход по цене, стоп за свингом (зажат между 0.6 и 2.2 ATR), T1 = 1.2R, T2 = 2.2R.
Строка **издержки** показывает, какую долю R съедают спред и комиссия: больше 0.15R — красным.
На минутке это решает чаще, чем направление.

## Калибровка

Таблица справа внизу: что реально случилось после каждой смены вердикта, разбитое по
уверенности (`< 45`, `45–60`, `60–75`, `≥ 75`). Сделок, винрейт, средний R за вычетом
издержек, среднее число баров.

Смотреть надо не на общий итог, а на **монотонность**: растёт ли средний R вместе
с уверенностью. Если нет — движок на этом инструменте не различает ситуации, и порог
крутить бесполезно. Симулятор входит по закрытию бара смены вердикта, при касании стопа
и цели на одном баре засчитывает стоп, держит не дольше тайм-стопа.

## Установка

1. TradingView → **Pine Editor** → **Открыть → Новый индикатор**.
2. Вставить `pine/COMPASS.pine`, **Сохранить**, **Добавить на график**.
3. Индикатор рисуется поверх свечей. Таймфрейм графика — 1 минута.

## Про перерисовку

Галка **«Без перерисовки»** включена по умолчанию: старшие ТФ берутся с предыдущего
**закрытого** бара своего таймфрейма, поэтому история на графике совпадает с тем, что
вы видели в реальном времени. Ценой одного бара запаздывания.

Выключите — и 5-минутный контекст начнёт обновляться внутри бара: реакция быстрее,
но прошлые сигналы на графике будут выглядеть красивее, чем были. Для оценки движка
по истории это бесполезно; для ручной торговли прямо сейчас — иногда удобно.

Всё остальное считается по закрытым данным текущего бара. VWAP считается вручную,
а не через `ta.vwap`, чтобы скрипт жил на инструментах без объёма: там веса заменяются
единицами, а фильтр RVOL сам отключается.

## Что стоит крутить

| параметр | зачем |
|---|---|
| **Порог смещения** (32) | ставится по таблице калибровки, а не по вкусу |
| **Порог сброса** (16) | гистерезис: выше — вердикт держится дольше, ниже — чаще мигает |
| **ТФ №1/№2/№3** (5/15/60) | под свой инструмент: для крипты уместнее 3/15/60 |
| **Минимальный перцентиль ATR** (25) | главный фильтр «сегодня нечего ловить» |
| **Издержки, тиков** | поставьте свои реальные — от них зависит вся калибровка |
| **Веса компонентов** | сумма должна оставаться равной 1, иначе поедет шкала и порог |
| **τ: ADX и ER** | где у вашего инструмента кончается диапазон и начинается тренд |

## Алерты

Четыре условия: `COMPASS · ЛОНГ`, `COMPASS · ШОРТ`, `COMPASS · нейтраль`,
`COMPASS · любая смена`. Плюс вызов `alert()` с полным планом — выбрать тип алерта
**«Любой вызов функции alert()»**, и в сообщение придёт сторона, скор, уверенность,
режим, плейбук, вход, стоп, T1, T2, риск и доля издержек.

Ставьте **«Раз за бар на закрытии»**.

## Оговорка

Веса и пороги по умолчанию — разумная отправная точка, а не результат оптимизации на ваших
данных. Их и надо проверить таблицей калибровки, прежде чем ставить деньги. Движок не
предсказывает цену: он раскладывает то, что уже на графике, в одно число и честно говорит,
когда это число ничего не значит.
